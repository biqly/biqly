global:
  config:
    BI_LOG_LEVEL: info
    BI_LOG_FORMAT: json

catalog:
  replicaCount: 3
  autoscaling:
    minReplicas: 3
    maxReplicas: 6

query:
  replicaCount: 4
  autoscaling:
    minReplicas: 4
    maxReplicas: 12

ai:
  replicaCount: 3
  autoscaling:
    minReplicas: 3
    maxReplicas: 10
