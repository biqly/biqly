global:
  imagePullSecrets:
    - name: ghcr-registry
  secrets:
    createSecrets: false
  config:
    BI_LOG_LEVEL: info
    BI_LOG_FORMAT: json
  observability:
    serviceMonitor:
      enabled: false
    prometheusRules:
      enabled: false
    alertmanager:
      enabled: false
    logs:
      vectorConfig:
        enabled: false
    tracing:
      otelCollector:
        enabled: false
      jaeger:
        enabled: false
    dashboards:
      enabled: true

catalog:
  replicaCount: 1
  autoscaling:
    minReplicas: 1
    maxReplicas: 2

query:
  replicaCount: 1
  autoscaling:
    minReplicas: 1
    maxReplicas: 2

ai:
  replicaCount: 1
  rollout:
    enabled: false
  autoscaling:
    minReplicas: 1
    maxReplicas: 2
