{{- if and .Values.global.gateway.enabled .Values.route.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "mcp.fullname" . }}
  labels:
    {{- include "mcp.labels" . | nindent 4 }}
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ .Values.global.gateway.parentRef.name }}
      namespace: {{ .Values.global.gateway.parentRef.namespace }}
      {{- with .Values.global.gateway.parentRef.sectionName }}
      sectionName: {{ . }}
      {{- end }}
    {{- with .Values.global.gateway.httpParentRef }}
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ .name }}
      namespace: {{ .namespace }}
      {{- with .sectionName }}
      sectionName: {{ . }}
      {{- end }}
    {{- end }}
  hostnames:
    {{- toYaml .Values.global.gateway.hostnames | nindent 4 }}
  rules:
    {{- range .Values.route.pathPrefixes }}
    - matches:
        - path:
            type: PathPrefix
            value: {{ . | quote }}
      backendRefs:
        - group: ""
          kind: Service
          name: {{ include "mcp.fullname" $ }}
          port: {{ $.Values.service.port }}
          weight: 1
    {{- end }}
{{- end }}
