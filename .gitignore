# Go
bin/
tmp/
/api
*.test
*.out
coverage.out
# Stray Go test binaries / coverage profiles left in the repo root by
# `go test -c` / `go test -coverprofile` (e.g. auth.test, app.test,
# workspace.test, handlers.test, rbac.test). Covered by *.test/*.out above,
# listed explicitly to document the intent and keep the workspace clean.
/auth.test
/app.test
/workspace.test
/handlers.test
/rbac.test
*.exe
semgrep.sarif

# Env / secrets
.env
.env.local
.env.dev
*.pem

# IDE
.idea/
.vscode/
.cursor/mcp.json

# OS
.DS_Store
Thumbs.db

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Python
__pycache__/
*.py[cod]

# Logs
*.log

# Helm dependency archives generated by `helm dependency build`
deploy/helm/biqly/charts/*.tgz

# Seed data (fetched via scripts/fetch-adventureworks.sh)
testdata/adventureworks/

.gograph/
